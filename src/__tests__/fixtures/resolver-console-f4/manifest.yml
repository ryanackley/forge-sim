app:
  id: ari:cloud:ecosystem::app/resolver-console-f4
  name: F4 Console Surface Repro
  runtime:
    name: nodejs22.x

modules:
  webtrigger:
    - key: noop-hook
      function: ping-fn
  function:
    - key: ping-fn
      handler: index.handler
