app:
  id: sim-app
  runtime:
    name: nodejs22.x
modules:
  macro:
    - key: react-dedup-test
      resource: main
      render: native
      title: React Dedup Test
      resolver:
        function: resolver
  function:
    - key: resolver
      handler: src/resolver.handler
resources:
  - key: main
    path: src/index.tsx
permissions:
  scopes: []
